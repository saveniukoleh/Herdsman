"use strict";
class FilterEffect {
  constructor(options) {
    this.pipe = "filter";
    this.priority = 1;
    this.filters = options?.filters;
    this.filterArea = options?.filterArea;
  }
  destroy() {
    for (let i = 0; i < this.filters.length; i++) {
      this.filters[i].destroy();
    }
    this.filters = null;
    this.filterArea = null;
  }
}
const filterEffectsPool = [];
function getFilterEffect(filters, filterArea) {
  const filterEffect = filterEffectsPool.pop() || new FilterEffect();
  filterEffect.filters = filters;
  filterEffect.filterArea = filterArea;
  return filterEffect;
}
function returnFilterEffect(effect) {
  effect.filters = null;
  filterEffectsPool.push(effect);
}

export { FilterEffect, getFilterEffect, returnFilterEffect };
//# sourceMappingURL=FilterEffect.mjs.map
